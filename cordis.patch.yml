# 单身汉（DSH）播放器 bundle 补丁：插入宿主半插件行。
# 同一个包的浏览器半由 dsh-client-modules 扫描 package.json 的 dsh.client 声明自动挂载，
# 从 /plugins/dsh-music-huazai/client.js 提供界面 bundle。
- insert:
    - id: music
      name: 'dsh-music-huazai'
